machine:
  node:
    version: 7.10.0
  environment:
    PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"

dependencies:
  override:
    - npm install -g npm@5
    - npm install

test:
  override:
    - npm test
